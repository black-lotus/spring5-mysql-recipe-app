spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dondon_dev
    username: dondon_dev_user
    password: dondon
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: dondon_database_create.sql
